#include <bits/stdc++.h>

using namespace std;

int n, l;
vector<int> conns[1000100];

void dfs(int w, int o) {
	
}

int main() {
	scanf("%d", &n);
	
	int a, b;
	for (int i = 1; i < n; i++) {
		scanf("%d %d", &a, &b);
	}
	
	dfs(1, 0);
	
	
	
	return 0;
}
