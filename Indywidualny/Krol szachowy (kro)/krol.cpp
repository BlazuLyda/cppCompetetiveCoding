#include <bits/stdc++.h>

using namespace std;

long long xp, yp, xk, yk;
long long wyn;
long long silnia[30300], silniaInv[30300];
long long d, h, l, r;
long long dec[15][7]; // Liczba skróconych dwójek i piątek z mianownikiem

int main() {
	scanf("%lld %lld %lld %lld", &yp, &xp, &yk, &xk);
	
	h = yk - yp;
	d = xk - xp;
	
	for (long long f = 0; f <= h; f++) {
		l = (h - f - d) / 2LL;
		r = l + d;
		
		if ()
		
	}
	
	return 0;
}
